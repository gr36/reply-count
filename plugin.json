{
    "version": "1.1.0",
    "title": "Show Reply Count",
    "description": "Add the reply count to your micro.blog posts.",
    "fields": [
        {
            "field": "params.reply_count_show_emoji",
            "label": "Show Emoji before the count",
            "type": "boolean"
          },
        {
          "field": "params.reply_count_emoji_before",
          "label": "Emoji before the count",
          "placeholder": "👋"
        },
        {
          "field": "params.reply_count_text_single",
          "label": "Text to appear after a single reply",
          "placeholder": "Reply"
        },
        {
          "field": "params.reply_count_text_multiple",
          "label": "Text to appear after multiple replies",
          "placeholder": "Replies"
        }
    ]
}
  